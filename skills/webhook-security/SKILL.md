---
name: webhook-security
description: Webhook security — signature verification, replay protection, IP allowlisting, payload validation
layer: domain
category: security
triggers: [webhook security, webhook signature, webhook verification, svix, webhook replay]
linksTo: [webhooks, encryption, owasp]
linkedFrom: []
riskLevel: low
---

# webhook-security

Webhook security — signature verification, replay protection, IP allowlisting, payload validation

## When to Use
Activate when the prompt involves: webhook security, webhook signature, webhook verification, svix, webhook replay

## Key Patterns

```typescript
// See documentation for webhook-security patterns
```

## Best Practices
- Follow established webhook-security conventions
- Test thoroughly before production
- Document decisions and trade-offs
