---
name: codexkit-auto-memory
description: Use when maintaining or operating CodexKit's hourly memory capture loop that promotes durable facts, preferences, and stable workflow learnings into local memory.
---

# codexkit-auto-memory

Use the hourly memory capture loop to promote stable, high-signal learnings into CodexKit memory. This is the canonical skill for scheduled memory capture behavior.

## Operating Notes
- Tool entrypoint: `~/.codex/tools/codexkit-auto-memory`
- Schedule target in this setup: every 1 hour.
- Capture only durable facts, preferences, decisions, and verified fixes.
- Exclude transient logs, speculative bug hypotheses, and noisy shell output.
